============
Installation
============

At the command line::

    $ easy_install xix-event-mib

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv xix-event-mib
    $ pip install xix-event-mib
